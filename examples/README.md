# Examples

This directory is reserved for runnable examples once the SDK grows beyond the
current package scaffold.

Planned example categories:

- one-shot `query()` usage
- stateful thread management with `CodexSDKClient`
- low-level `AppServerClient` usage against `codex app-server`
- approval-handling flows
- structured-output workflows
