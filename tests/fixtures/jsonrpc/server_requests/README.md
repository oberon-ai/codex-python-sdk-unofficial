# JSON-RPC Server Request Fixtures

Store one server-initiated request envelope per file in this directory. This is
where approval requests and similar protocol flows should live.

Naming convention: `NNN_event_name.server_request.json`

Example: `020_approval_required.server_request.json`
