# JSON-RPC Response Fixtures

Store one server response envelope per file in this directory. Keep the numeric
prefix aligned with the request fixture that produced it.

Naming convention: `NNN_method_name.response.json`

Example: `001_initialize.response.json`
