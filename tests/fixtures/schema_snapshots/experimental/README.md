# Experimental Schema Snapshots

Store experimental upstream schema snapshots in this directory. Keeping them
separate prevents stable tests from silently depending on experimental fields or
methods.

Suggested filename: `codex_app_server_protocol.v2.experimental.schemas.json`
